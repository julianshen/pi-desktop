import type { CSSProperties } from "react";

/** Shared muted/secondary text color used across the settings views. */
export const mutedText: CSSProperties = { color: "color-mix(in srgb, var(--color-text) 55%, transparent)" };
